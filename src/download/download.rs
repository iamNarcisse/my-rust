use std::thread;

fn download_from_net_thread() {
    // Spawn two threads to do work.
    // let thread_one = thread::spawn(|| download("https://fandksavings.com"));
    // let thread_two = thread::spawn(|| download("https://cowrywise.com"));

    // Wait for both threads to complete

    // thread_one.join().expect("Thread one panicked");
    // thread_two.join().expect("Thread two panicked");
}
